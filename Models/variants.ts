export interface Variants {
  hidden: {
    opacity: number;
    maxHeight: string;
    transform: string;
  };
  exit: {
    opacity: number;
    maxHeight: string;
    transform: string;
  };
  show: {
    opacity: number;
    maxHeight: string;
    transform: string;
  }
}